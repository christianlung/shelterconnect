import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    // TODO Just for testing with some images
    domains: ['encrypted-tbn0.gstatic.com'],
  },
};

export default nextConfig;
